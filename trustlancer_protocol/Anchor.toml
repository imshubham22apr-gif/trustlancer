[toolchain]
package_manager = "yarn"
# anchor_version = "0.30.1" # Commented out to use system anchor

[features]
resolution = true
skip-lint = false

[programs.devnet]
solanahub_protocol = "F65dyNspaW3225BEuJh2W7wnigtdVjxnmeFxXPFr5Rpm"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 \"tests/**/*.ts\""

[test]
startup_wait = 60000
shutdown_wait = 2000
upgradeable = false
